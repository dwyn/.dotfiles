- defaults:
    create: true
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.tmux.conf:
    # ~/.vim: 
    # ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Development/Projects

- shell:
    # command: ./setup_homebrew.zsh
    -
     command: ./setup_homebrew.zsh
     stdout: true
     sterr: true
     description: INSTALL BREW
     quiet: false
